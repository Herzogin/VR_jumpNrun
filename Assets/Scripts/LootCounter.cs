using UnityEngine;

public class LootCounter : MonoBehaviour
{
    public static int lootCount = 0;
    //public static bool enoughLoot = false;

    // Update is called once per frame
    void Update()
    {
        //if (lootCount > 10)
        //{
        //    enoughLoot = true;
        //}
    }
}
